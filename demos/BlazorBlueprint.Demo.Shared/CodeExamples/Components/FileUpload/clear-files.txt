<BbFileUpload @ref="uploader" @bind-Files="files" Multiple="true" />
<BbButton Variant="ButtonVariant.Outline"
          OnClick="() => uploader.ClearFiles()"
          Disabled="@(files is null || files.Count == 0)">
    Clear Files
</BbButton>

@code {
    private BbFileUpload? uploader;
    private IReadOnlyList<FileUploadItem>? files;
}